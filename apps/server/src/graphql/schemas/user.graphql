type Query {
  login(email: String!, passowrd: String!): AuthenticateResponse!
}

type Mutation {
  register(email: String!, password: String!): AuthenticateResponse!
}

type AuthenticateResponse {
  token: String
}
